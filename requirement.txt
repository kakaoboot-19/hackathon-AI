fastapi==0.104.1            # FastAPI: 웹 프레임워크
uvicorn[standard]==0.24.0   # Uvicorn: ASGI 서버 (내장 톰캣 같은 역할)
boto3==1.34.19              # boto3: AWS SDK for Python (S3 업로드용)
python-dotenv==1.0.0        # python-dotenv: .env 파일에서 환경변수 로드
requests